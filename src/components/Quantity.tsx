export function Quantity({value,onChange,label='Quantity'}:{value:number;onChange:(n:number)=>void;label?:string}){return <label className="quantity"><span>{label}</span><div><button type="button" onClick={()=>onChange(Math.max(1,value-1))} aria-label={`Decrease ${label}`}>−</button><input aria-label={label} inputMode="numeric" value={value} onChange={e=>{const v=e.target.value;if(/^\d*$/.test(v))onChange(v===''?0:Number(v))}}/><button type="button" onClick={()=>onChange(value+1)} aria-label={`Increase ${label}`}>+</button></div></label>}
