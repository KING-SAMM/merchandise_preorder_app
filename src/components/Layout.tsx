import { Link,NavLink,useNavigate } from 'react-router-dom'; import { useEffect,useState } from 'react'; import { useOrder } from '../state/OrderContext';
export function Layout({children}:{children:React.ReactNode}){const {order,clear}=useOrder();const [open,setOpen]=useState(false);const [dark,setDark]=useState(()=>localStorage.getItem('ctc-theme')==='dark');const nav=useNavigate();useEffect(()=>{document.documentElement.dataset.theme=dark?'dark':'light';localStorage.setItem('ctc-theme',dark?'dark':'light')},[dark]);const count=Object.keys(order.selections).length;const start=()=>{if(confirm('Start a new order? Your current selections and member details will be cleared.')){clear();nav('/merchandise')}};return <><a className="skip" href="#main">Skip to content</a><header><Link className="brand" to="/">CTC <span>CREATIVE TIME CENTER</span></Link><button className="menu" onClick={()=>setOpen(!open)} aria-expanded={open}>Menu</button><nav className={open?'open':''}>{[['/merchandise','Merchandise'],['/t-shirts','T-Shirts'],['/corporate-shirts','Corporate Shirts'],['/hoodies','Hoodies'],['/caps','Caps'],['/selection',`My Selection${count?` (${count})`:''}`]].map(([to,label])=><NavLink key={to} to={to} onClick={()=>setOpen(false)}>{label}</NavLink>)}<button className="text-button" onClick={()=>setDark(!dark)}>{dark?'Light mode':'Dark mode'}</button><button className="text-button" onClick={start}>Start New Order</button></nav></header><main id="main">{children}</main><footer><span>Creative Time Center · CTC 2nd Year Anniversary</span><span>From Vision to Global Movement · No payment collected here</span></footer></>}
