import type { OrderState } from '../types'; export const validateOrder=(o:OrderState)=>{const e:string[]=[];const rows=Object.values(o.selections);if(!rows.length)e.push('Choose at least one merchandise item.');rows.forEach(x=>{if(!x||!Number.isInteger(x.quantity)||x.quantity<1)e.push('Choose a whole quantity of at least 1.');if(x&&x.productType!=='cap'&&!x.size)e.push('Choose a clothing size.');});if(!o.member.profileName.trim())e.push('Profile Name is required.');if(!o.member.minerId.trim())e.push('Miner ID is required.');if(o.member.email&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(o.member.email))e.push('Enter a valid email address.');return e};
