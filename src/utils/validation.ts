import type { OrderState } from '../types';
export const validateOrder=(o:OrderState)=>{const e:string[]=[];if(!o.shirt&&!o.cap)e.push('Choose at least one merchandise item.');if(o.shirt&&(!o.shirt.size||!Number.isInteger(o.shirt.quantity)||o.shirt.quantity<1))e.push('Choose a shirt size and whole quantity of at least 1.');if(o.cap&&(!Number.isInteger(o.cap.quantity)||o.cap.quantity<1))e.push('Choose a whole cap quantity of at least 1.');if(!o.member.profileName.trim())e.push('Profile Name is required.');if(!o.member.minerId.trim())e.push('Miner ID is required.');if(o.member.email&& !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(o.member.email))e.push('Enter a valid email address.');return e};
